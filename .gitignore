node_modules/

# NOT: android/ klasörünün tamamı bilinçli olarak commit edilmiyor.
# GitHub Actions her build'de "npx cap add android" ile sıfırdan üretiyor,
# ardından scripts/configure-android.mjs yapılandırıyor. Bu yüzden build
# çıktıları değil, android/ klasörünün TAMAMI yok sayılıyor.
android/

# Secrets — hiçbir zaman commit edilmemeli
*.jks
*.keystore
.env
config/admob.json

# OS / editor
.DS_Store
.idea/
*.swp

# Design reference files (tasarım referansları, üretim kodu değil)
design-references/
