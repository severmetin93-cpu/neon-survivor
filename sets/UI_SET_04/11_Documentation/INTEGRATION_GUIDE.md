# Technology Integration

Bind nodes to the real progression graph.
Bind prerequisite checks to unlocked node IDs.
Bind level/cost values to the economy.
Update node state dynamically: locked, available, active, maxed.
Use connection assets only as visual layers; the actual graph must remain data-driven.
Validate scrolling/panning and touch selection on Android.
