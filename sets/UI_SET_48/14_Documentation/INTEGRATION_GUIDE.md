# Notification Integration

Notification state must be synchronized with authoritative services. Read/unread state may be user-specific. Reward claims must be idempotent and server-validated. Expired mail remains visible when useful for history but cannot be claimed. News/events/announcements should use server-provided publish and expiry timestamps. System-critical notices should respect platform and in-game notification preferences where appropriate.
