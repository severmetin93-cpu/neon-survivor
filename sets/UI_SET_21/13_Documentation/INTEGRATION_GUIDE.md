# Map Integration

World and sector unlock state must come from progression.
Node connections must match the authoritative world graph.
Location threat/power values must come from content definitions.
Markers should bind to stable location/objective IDs.
Fast travel must validate discovery, destination availability, cooldown and any costs before travel.
The map UI must never unlock a location merely because its marker is rendered.
