# Inventory Integration

Ownership and quantities must be authoritative. Equip/use/drop operations must validate ownership, slot compatibility, level requirements, inventory capacity and cooldowns. Loadouts should be persisted atomically. Sort/filter/search are presentation operations and must not mutate inventory state.
