# Quest Integration

Quest definitions and objective progress must come from authoritative game state. Daily/weekly reset timestamps must be server-derived. Reward claims must be validated and idempotent. Quest tracking should update map waypoints without altering quest state. Failed/expired quests must remain non-claimable.
