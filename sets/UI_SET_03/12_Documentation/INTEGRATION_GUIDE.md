# Inventory Integration

Bind the item grid to the runtime inventory collection.
Bind rarity frames to item rarity.
Bind equipment slots to the player's equipped item IDs.
Bind upgrade values/costs to the actual progression system.
Keep primary touch targets >= 48dp.
Use filters/sort/search without changing item identity.
Validate inventory scrolling, equip, unequip, compare, trash and upgrade flows.
