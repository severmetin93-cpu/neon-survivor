# Social Integration

Friend relationships, party membership, clan membership and presence are authoritative. Invites require server validation and expiry handling. Block/mute/report state must be enforced across chat and voice. Moderation reports should not expose private moderation outcomes beyond permitted status. Reconnect must reconcile party, presence and voice state.
